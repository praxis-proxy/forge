[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
publish = false
description = "Developer-tooling automation for forge, run via `cargo xtask <task>`."

[[bin]]
name = "xtask"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
anyhow = "1.0"
regex = "1.11"
